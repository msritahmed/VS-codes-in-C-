#include<stdio.h>
int main()
{
    int a;
    int sum=1;

    printf("enter the number to find factorial\n");
    scanf("%d",&a);
     
    for(int i=1;i<=a;i++)
    sum*=i;
    
    printf("factorial of %d is :%d\n",a,sum);
    return 0;

}
